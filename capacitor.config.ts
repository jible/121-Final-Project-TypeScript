import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'team2.final.app',
    appName: '121-final-team-2',
    webDir: './dist/',
}

export default config
